{
  "name": "membrane-sfu",
  "version": "0.1.0",
  "description": "Client library for Membrane SFU.",
  "license": "Apache 2.0",
  "author": "Membrane Team",
  "repository": {
    "type": "git",
    "url": "git://github.com/membraneframework/membrane_sfu.git"
  },
  "scripts": {
    "ts-types": " tsc --emitDeclarationOnly --outDir ../priv/static",
    "build": "rimraf lib && node ./esbuild.js && npm run ts-types",
    "docs": "typedoc js/index.ts --out ../doc/js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "esbuild": "^0.12.23",
    "eslint": "^7.21.0",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "typedoc": "^0.21.2",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "@types/uuid": "^8.3.1",
    "uuid": "^8.3.2"
  }
}
